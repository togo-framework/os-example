# weather

A togo plugin (mini-app) for [togo](https://github.com/togo-framework/togo).

```bash
togo install <owner>/weather
```

Exposes `GET /api/weather/ping` and injects `web/app/weather/page.tsx`.
